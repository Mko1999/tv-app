# React + TypeScript + Vite (Test Task)

This repository is a **test task** using React + TypeScript + Vite with HMR and ESLint.

## Scripts

- **Run dev:** `npm run dev`
- **Lint:** `npm run lint`
- **Build:** `npm run build`
